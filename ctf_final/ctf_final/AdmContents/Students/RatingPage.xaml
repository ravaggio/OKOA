<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:behavior="clr-namespace:ctf_final.Behaviors"
             mc:Ignorable="d"
             x:Class="ctf_final.AdmContents.Students.RatingPage">
    <ContentPage.Resources>
        <ResourceDictionary>
            <Style TargetType="Label">
                <Setter Property="TextColor" Value="{StaticResource Orange}"/>
                <Setter Property="FontSize" Value="Medium"/>
                <Setter Property="VerticalOptions" Value="CenterAndExpand"/>
                <Setter Property="Margin" Value="10, 0, 0, 0"/>
            </Style>
            <Style TargetType="Entry">
                <Setter Property="Keyboard" Value="Numeric" />
                <Setter Property="TextColor" Value="{StaticResource TextLight}" />
                <Setter Property="VerticalOptions" Value="End" />
                <Setter Property="Margin" Value="0, 0, 10, 0"/>
            </Style>
            <Style TargetType="Button">
                <Setter Property="BackgroundColor" Value="{StaticResource Orange}"/>
                <Setter Property="TextColor" Value="{StaticResource TextDark}" />
            </Style>
        </ResourceDictionary>
    </ContentPage.Resources>
    <ContentPage.Content>
        <AbsoluteLayout>
            <Image Source="rating_bg.png" Aspect="AspectFill"
                   AbsoluteLayout.LayoutBounds="0, 0, 1, 1"
                   AbsoluteLayout.LayoutFlags="All"/>
            <ActivityIndicator IsVisible="True" IsRunning="True" x:Name="loadingSign" AbsoluteLayout.LayoutBounds="0.5, 0.5, -1, -1" AbsoluteLayout.LayoutFlags="PositionProportional"/>
            <StackLayout x:Name="layout" IsVisible="False" Spacing="10" AbsoluteLayout.LayoutBounds="0.5, 0.5, -1, -1" AbsoluteLayout.LayoutFlags="PositionProportional">
                <Grid Margin="10, 12, 10, 12" RowSpacing="0" BackgroundColor="{StaticResource DarkTransparent}" VerticalOptions="Center" HorizontalOptions="Center" >
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <BoxView BackgroundColor="{StaticResource Orange}" Grid.ColumnSpan="2"/>
                    <BoxView BackgroundColor="{StaticResource DarkTransparent}" Grid.Row="1" Grid.RowSpan="5"/>
                    <Label Grid.Row="0" Grid.ColumnSpan="2" VerticalOptions="Center" HorizontalOptions="Center" Text="AVALIAÇÃO" TextColor="{StaticResource TextDark}"/>
                    <Label Grid.Row="1" Grid.Column="0" Text="% Gordura: " Margin="10, 10, 0, 0"/>
                    <Entry Grid.Row="1" Grid.Column="1" x:Name="fatEntry" BackgroundColor="Transparent" Placeholder="0%" Text="0%" Margin="0, 10, 10, 0">
                        <Entry.Behaviors>
                            <behavior:DotBehaviour AddPercentage="True"/>
                        </Entry.Behaviors>
                    </Entry>
                    <Label Grid.Row="2" Grid.Column="0" Text="% Massa magra: " />
                    <Entry Grid.Row="2" Grid.Column="1" BackgroundColor="Transparent" x:Name="massEntry" Placeholder="0%" Text="0%">
                        <Entry.Behaviors>
                            <behavior:DotBehaviour AddPercentage="True"/>
                        </Entry.Behaviors>
                    </Entry>
                    <Label Grid.Row="3" Grid.Column="0" Text="Peso (kg): " />
                    <Entry Grid.Row="3" Grid.Column="1" BackgroundColor="Transparent" x:Name="weightEntry" Placeholder="0.0">
                        <Entry.Behaviors>
                            <behavior:DotBehaviour/>
                        </Entry.Behaviors>
                    </Entry>
                    <Label Grid.Row="4" Grid.Column="0" Text="Altura (m): "/>
                    <Entry x:Name="heightEntry" BackgroundColor="Transparent" Grid.Row="4" Grid.Column="1" Placeholder="0.00">
                        <Entry.Behaviors>
                            <behavior:MakedEntryBehavior Mask="X.XX"/>
                        </Entry.Behaviors>
                    </Entry>
                    <Label Margin="10,0,0,10" Grid.Row="5" Grid.Column="0" Text="Mobilidade: "/>
                    <Picker Margin="0,0,10,10" Grid.Row="5" Grid.Column="1" x:Name="mobilityPicker" TextColor="{StaticResource TextLight}">
                        <Picker.ItemsSource>
                            <x:Array Type="{x:Type x:String}">
                                <x:String>Pouco mobilizado</x:String>
                                <x:String>Bem mobilizado</x:String>
                                <x:String>Hiper mobilizado</x:String>
                            </x:Array>
                        </Picker.ItemsSource>
                        <Picker.BackgroundColor>
                            <OnPlatform x:TypeArguments="Color">
                                <OnPlatform.Platforms>
                                    <On Platform="iOS" Value="{StaticResource PrimaryDark}" />
                                    <On Platform="Android" Value="Transparent" />
                                </OnPlatform.Platforms>
                            </OnPlatform>
                        </Picker.BackgroundColor>
                    </Picker>
                </Grid>
                <Button Margin="10, 0, 10, 0" Text="ADICIONAR" Clicked="CreateRating"/>
            </StackLayout>
        </AbsoluteLayout>
    </ContentPage.Content>
</ContentPage>