<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:converters="clr-namespace:ctf_final.Converters"
             xmlns:controls="clr-namespace:ImageCircle.Forms.Plugin.Abstractions;assembly=ImageCircle.Forms.Plugin"
             mc:Ignorable="d"
             x:Class="ctf_final.AdmContents.Students.StudentsSelectionPage"
             Title="Selecionar Aluno">
    <ContentPage.Resources>
        <converters:PictureConverter x:Key="PicConverter" />
    </ContentPage.Resources>
    <ContentPage.Content>
        <AbsoluteLayout>
            <Image Source="rating_bg.png" Aspect="AspectFill"
                   AbsoluteLayout.LayoutBounds="0, 0, 1, 1"
                   AbsoluteLayout.LayoutFlags="All"/>
            <StackLayout x:Name="viewLayout" Spacing="0" BackgroundColor="{StaticResource DarkTransparent}" AbsoluteLayout.LayoutBounds="0, 0, 1, 1" AbsoluteLayout.LayoutFlags="All">
                <SearchBar x:Name="searchBar" BackgroundColor="{StaticResource LightTransparent}" Placeholder="Buscar aluno... " TextChanged="SearchBar_TextChanged" TextColor="{StaticResource Orange}" PlaceholderColor="DarkGray">
                    <SearchBar.BackgroundColor>
                        <OnPlatform x:TypeArguments="Color">
                            <On Platform="iOS" Value="{StaticResource PrimaryDark}"/>
                            <On Platform="Android" Value="{StaticResource LightTransparent}"/>
                        </OnPlatform>
                    </SearchBar.BackgroundColor>
                </SearchBar>
                <ListView ItemTapped="OnStudentSelected" 
                          SelectionMode="None"
                          Footer="" 
                          HasUnevenRows="True" 
                          BackgroundColor="Transparent"
                          x:Name="studentsList" 
                          SeparatorColor="{StaticResource LightTransparent}">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <ViewCell>
                                <StackLayout Orientation="Horizontal" Spacing="6" Padding="6">
                                    <controls:CircleImage WidthRequest="50" HeightRequest="50"
                                                          Aspect="AspectFill"
                                                          Source="{Binding PictureToken, Converter={StaticResource PicConverter}}"/>
                                    <Label Text="{Binding Name}" FontSize="Medium" VerticalOptions="CenterAndExpand" TextColor="{StaticResource Orange}"/>
                                </StackLayout>
                            </ViewCell>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackLayout>
        </AbsoluteLayout>
    </ContentPage.Content>
</ContentPage>