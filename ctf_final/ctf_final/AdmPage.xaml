<?xml version="1.0" encoding="utf-8" ?>
<MasterDetailPage
            xmlns="http://xamarin.com/schemas/2014/forms"
            xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
            xmlns:d="http://xamarin.com/schemas/2014/forms/design"
            xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
            xmlns:me="clr-namespace:ctf_final.MarkupExtensions"
            xmlns:controls="clr-namespace:ImageCircle.Forms.Plugin.Abstractions;assembly=ImageCircle.Forms.Plugin"
            mc:Ignorable="d"
            x:Class="ctf_final.AdmPage"
            MasterBehavior="Popover">
    <MasterDetailPage.Master>
        <ContentPage Title="≡" NavigationPage.HasNavigationBar="false">
            <ContentPage.Resources>
                <ResourceDictionary>
                    <Style TargetType="Button">
                        <Setter Property="Margin" Value="0" />
                        <Setter Property="BackgroundColor" Value="{StaticResource Primary}" />
                        <Setter Property="TextColor" Value="{StaticResource Orange}"/>
                        <Setter Property="FontSize" Value="Medium"/>
                    </Style>
                    <Style TargetType="Label">
                        <Setter Property="TextColor" Value="{StaticResource TextLight}" />
                    </Style>
                </ResourceDictionary>
            </ContentPage.Resources>
            <ContentPage.Content>
                <StackLayout Spacing="10" BackgroundColor="{StaticResource Primary}" VerticalOptions="Fill">
                    <StackLayout.Padding>
                        <OnPlatform x:TypeArguments="Thickness">
                            <On Platform="Android, UWP">0</On>
                            <On Platform="iOS">0,20,0,0</On>
                        </OnPlatform>
                    </StackLayout.Padding>
                    <StackLayout x:Name="menuHeader" Orientation="Horizontal" BackgroundColor="{StaticResource PrimaryDark}" Padding="10, 25, 10, 10">
                        <controls:CircleImage WidthRequest="60" HeightRequest="60" 
                                      Source="{me:EmbeddedImage ctf_final.Images.empty.jpg}"/>
                        <StackLayout Padding="8, 4, 8, 4" Spacing="0" VerticalOptions="EndAndExpand">
                            <Label Text="{Binding Name}" FontSize="Medium" />
                            <Label Text="{Binding Function}" FontSize="Micro"/>
                        </StackLayout>
                    </StackLayout>
                    <StackLayout Spacing="0" VerticalOptions="StartAndExpand">
                        <ListView x:Name="menuList" SeparatorVisibility="None" SelectionMode="None" ItemTapped="MenuList_ItemTapped" HasUnevenRows="True" BackgroundColor="Transparent">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <ViewCell>
                                        <StackLayout Padding="12, 8, 0, 8" Orientation="Horizontal">
                                            <Image Margin="6, 0, 0, 0" Source="{Binding IconSource}" Aspect="AspectFit"/>
                                            <Label Margin="6, 0, 0, 0"
                                                   FontSize="Medium"
                                                   TextColor="{StaticResource Orange}"
                                                   HorizontalOptions="StartAndExpand" 
                                                   VerticalOptions="CenterAndExpand" 
                                                   Text="{Binding Label}"/>
                                        </StackLayout>
                                    </ViewCell>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </StackLayout>
                </StackLayout>
            </ContentPage.Content>
        </ContentPage>
    </MasterDetailPage.Master>
    <MasterDetailPage.Detail>
        <ContentPage/>
    </MasterDetailPage.Detail>
</MasterDetailPage>

